metadata:
  version: "1.0.0"
  created: "2026-05-10"
  author: "PAIML Engineering"
  description: "Fine-tuning recipe t4_grpo_math: grpo on qwen3"
  references:
    - "docs/specifications/fine-tuning-cookbook.md"
    - "docs/specifications/fine-tuning-cookbook/manifest.yaml#t4_grpo_math"
  depends_on:
    - "recipe-iiur-v1"
  tags:
    - finetune
    - tier_4
    - grpo
    - qwen3

kernel_structure:
  phases:
    - name: setup
      description: "Construct RecipeContext; load fixture"
      invariant: "context.temp_dir empty; fixture path readable"
    - name: train
      description: "Run apr finetune (or technique-specific entry point)"
      invariant: "Training is deterministic for fixed seed=42"
    - name: evaluate
      description: "Run the recipe metric (loss, perplexity, accuracy, etc.)"
      invariant: "Metric is finite; no NaN/Inf"
    - name: verify
      description: "Assert the falsifier"
      invariant: "Two consecutive runs produce equal verdict"
    - name: teardown
      description: "Drop RecipeContext"
      invariant: "context.temp_dir cleaned up"

equations:
  totality:
    formula: "main() returns Result<()> for any well-formed fixture"
    domain: "fixture: Path"
    codomain: "Result<()>"
    invariants:
      - "main() is total — no panic for any well-formed input"
    preconditions:
      - "fixture is valid UTF-8 JSONL or CSV"
    postconditions:
      - "Result is Ok or Err; no panic"
    lean_theorem: "ProvableContracts.Finetune.T4GrpoMath.Totality"
    tolerance: 0
    lean:
      theorem: "ProvableContracts.Finetune.T4GrpoMath.Totality"
      status: proved
      module: "lean/ProvableContracts/Finetune/T4GrpoMath.lean"

  determinism:
    formula: "main() with seed=42 produces equal output across runs"
    domain: "seed: u64, fixture: Path"
    codomain: "verdict: Hash"
    invariants:
      - "Two consecutive main() calls with seed=42 produce byte-equal output"
    preconditions:
      - "seed is fixed; fixture is identical"
    postconditions:
      - "verdict_1 == verdict_2"
    lean_theorem: "ProvableContracts.Finetune.T4GrpoMath.Determinism"
    tolerance: 0
    lean:
      theorem: "ProvableContracts.Finetune.T4GrpoMath.Determinism"
      status: proved
      module: "lean/ProvableContracts/Finetune/T4GrpoMath.lean"

  convergence:
    formula: "GRPO on math problems with verifiable rewards: pass@1 increases over 50 steps"
    domain: "fixture: Path, training_steps: u32"
    codomain: "metric: f32"
    invariants:
      - "GRPO on math problems with verifiable rewards: pass@1 increases over 50 steps"
    preconditions:
      - "fixture is bounded, deterministic"
    postconditions:
      - "the falsifiable claim above holds on the bundled fixture"
    lean_theorem: "ProvableContracts.Finetune.T4GrpoMath.Convergence"
    tolerance: 0
    lean:
      theorem: "ProvableContracts.Finetune.T4GrpoMath.Convergence"
      status: not-applicable
      module: "lean/ProvableContracts/Finetune/T4GrpoMath.lean"

proof_obligations:
  - type: invariant
    property: "main() is total"
    formal: "for all fixture: Path. main() ↦ Result<()> (no panic)"
    tolerance: 0.0
    applies_to: totality
    lean:
      theorem: "ProvableContracts.Finetune.T4GrpoMath.Totality"
      status: proved
      module: "ProvableContracts.Finetune.T4GrpoMath"
  - type: invariant
    property: "main() is deterministic"
    formal: "main(seed=42) == main(seed=42)"
    tolerance: 0.0
    applies_to: determinism
    lean:
      theorem: "ProvableContracts.Finetune.T4GrpoMath.Determinism"
      status: proved
      module: "ProvableContracts.Finetune.T4GrpoMath"
  - type: invariant
    property: "GRPO on math problems with verifiable rewards: pass@1 increases over 50 steps"
    formal: "GRPO on math problems with verifiable rewards: pass@1 increases over 50 steps"
    tolerance: 0.0
    applies_to: convergence
    lean:
      theorem: "ProvableContracts.Finetune.T4GrpoMath.Convergence"
      status: not-applicable
      module: "ProvableContracts.Finetune.T4GrpoMath"

falsification_tests:
  - id: FALSIFY-FT-T4_GRPO_MATH-001
    rule: "main() is total on the bundled fixture"
    prediction: "main() returns Ok(()) on the bundled fixture"
    test: "cargo test --example t4_grpo_math -- recipe_runs"
    if_fails: "Recipe panics or returns Err on the bundled fixture"
  - id: FALSIFY-FT-T4_GRPO_MATH-002
    rule: "Two consecutive runs produce equal output (determinism)"
    prediction: "main() with seed=42 produces equal verdict across two runs"
    test: "cargo test --example t4_grpo_math -- deterministic_across_runs"
    if_fails: "Non-determinism leaked (clock, RNG, threading)"
  - id: FALSIFY-FT-T4_GRPO_MATH-003
    rule: "GRPO on math problems with verifiable rewards: pass@1 increases over 50 steps"
    prediction: "GRPO on math problems with verifiable rewards: pass@1 increases over 50 steps"
    test: "cargo test --example t4_grpo_math -- falsifier_holds_on_fixture"
    if_fails: "grpo regression on bundled fixture"

kani_harnesses:
  - id: KANI-FT-T4_GRPO_MATH-001
    obligation: "main() is total"
    bound: 4
    strategy: bounded_int
    harness: "kani_harnesses::ft_t4_grpo_math_total"
  - id: KANI-FT-T4_GRPO_MATH-002
    obligation: "main() is deterministic"
    bound: 4
    strategy: bounded_int
    harness: "kani_harnesses::ft_t4_grpo_math_deterministic"
  - id: KANI-FT-T4_GRPO_MATH-003
    obligation: "GRPO on math problems with verifiable rewards: pass@1 increases over 50 steps"
    bound: 4
    strategy: bounded_int
    harness: "kani_harnesses::ft_t4_grpo_math_convergence"

qa_gate:
  id: F-FT-T4_GRPO_MATH-001
  name: "Fine-tuning t4_grpo_math contract"
  description: "grpo on qwen3: GRPO on math problems with verifiable rewards: pass@1 increases over 50 steps"
  checks:
    - main_is_total
    - deterministic_across_runs
    - falsifier_holds_on_fixture
  pass_criteria: "All 3 falsification tests pass on bundled fixture"
