metadata:
  version: "1.0.0"
  created: "2026-05-10"
  author: "PAIML Engineering"
  description: "Fine-tuning recipe t2_lora_aqlm: lora_quantized on llama"
  references:
    - "docs/specifications/fine-tuning-cookbook.md"
    - "docs/specifications/fine-tuning-cookbook/manifest.yaml#t2_lora_aqlm"
  depends_on:
    - "recipe-iiur-v1"
  tags:
    - finetune
    - tier_2
    - lora_quantized
    - llama

kernel_structure:
  phases:
    - name: setup
      description: "Construct RecipeContext; load fixture"
      invariant: "context.temp_dir empty; fixture path readable"
    - name: train
      description: "Run apr finetune (or technique-specific entry point)"
      invariant: "Training is deterministic for fixed seed=42"
    - name: evaluate
      description: "Run the recipe metric (loss, perplexity, accuracy, etc.)"
      invariant: "Metric is finite; no NaN/Inf"
    - name: verify
      description: "Assert the falsifier"
      invariant: "Two consecutive runs produce equal verdict"
    - name: teardown
      description: "Drop RecipeContext"
      invariant: "context.temp_dir cleaned up"

equations:
  totality:
    formula: "main() returns Result<()> for any well-formed fixture"
    domain: "fixture: Path"
    codomain: "Result<()>"
    invariants:
      - "main() is total — no panic for any well-formed input"
    preconditions:
      - "fixture is valid UTF-8 JSONL or CSV"
    postconditions:
      - "Result is Ok or Err; no panic"
    lean_theorem: "ProvableContracts.Finetune.T2LoraAqlm.Totality"
    tolerance: 0
    lean:
      theorem: "ProvableContracts.Finetune.T2LoraAqlm.Totality"
      status: proved
      module: "lean/ProvableContracts/Finetune/T2LoraAqlm.lean"

  determinism:
    formula: "main() with seed=42 produces equal output across runs"
    domain: "seed: u64, fixture: Path"
    codomain: "verdict: Hash"
    invariants:
      - "Two consecutive main() calls with seed=42 produce byte-equal output"
    preconditions:
      - "seed is fixed; fixture is identical"
    postconditions:
      - "verdict_1 == verdict_2"
    lean_theorem: "ProvableContracts.Finetune.T2LoraAqlm.Determinism"
    tolerance: 0
    lean:
      theorem: "ProvableContracts.Finetune.T2LoraAqlm.Determinism"
      status: proved
      module: "lean/ProvableContracts/Finetune/T2LoraAqlm.lean"

  convergence:
    formula: "AQLM 2-bit base + LoRA: post-merge perplexity ≤ FP16 baseline + 10%"
    domain: "fixture: Path, training_steps: u32"
    codomain: "metric: f32"
    invariants:
      - "AQLM 2-bit base + LoRA: post-merge perplexity ≤ FP16 baseline + 10%"
    preconditions:
      - "fixture is bounded, deterministic"
    postconditions:
      - "the falsifiable claim above holds on the bundled fixture"
    lean_theorem: "ProvableContracts.Finetune.T2LoraAqlm.Convergence"
    tolerance: 0
    lean:
      theorem: "ProvableContracts.Finetune.T2LoraAqlm.Convergence"
      status: not-applicable
      module: "lean/ProvableContracts/Finetune/T2LoraAqlm.lean"

proof_obligations:
  - type: invariant
    property: "main() is total"
    formal: "for all fixture: Path. main() ↦ Result<()> (no panic)"
    tolerance: 0.0
    applies_to: totality
    lean:
      theorem: "ProvableContracts.Finetune.T2LoraAqlm.Totality"
      status: proved
      module: "ProvableContracts.Finetune.T2LoraAqlm"
  - type: invariant
    property: "main() is deterministic"
    formal: "main(seed=42) == main(seed=42)"
    tolerance: 0.0
    applies_to: determinism
    lean:
      theorem: "ProvableContracts.Finetune.T2LoraAqlm.Determinism"
      status: proved
      module: "ProvableContracts.Finetune.T2LoraAqlm"
  - type: invariant
    property: "AQLM 2-bit base + LoRA: post-merge perplexity ≤ FP16 baseline + 10%"
    formal: "AQLM 2-bit base + LoRA: post-merge perplexity ≤ FP16 baseline + 10%"
    tolerance: 0.0
    applies_to: convergence
    lean:
      theorem: "ProvableContracts.Finetune.T2LoraAqlm.Convergence"
      status: not-applicable
      module: "ProvableContracts.Finetune.T2LoraAqlm"

falsification_tests:
  - id: FALSIFY-FT-T2_LORA_AQLM-001
    rule: "main() is total on the bundled fixture"
    prediction: "main() returns Ok(()) on the bundled fixture"
    test: "cargo test --example t2_lora_aqlm -- recipe_runs"
    if_fails: "Recipe panics or returns Err on the bundled fixture"
  - id: FALSIFY-FT-T2_LORA_AQLM-002
    rule: "Two consecutive runs produce equal output (determinism)"
    prediction: "main() with seed=42 produces equal verdict across two runs"
    test: "cargo test --example t2_lora_aqlm -- deterministic_across_runs"
    if_fails: "Non-determinism leaked (clock, RNG, threading)"
  - id: FALSIFY-FT-T2_LORA_AQLM-003
    rule: "AQLM 2-bit base + LoRA: post-merge perplexity ≤ FP16 baseline + 10%"
    prediction: "AQLM 2-bit base + LoRA: post-merge perplexity ≤ FP16 baseline + 10%"
    test: "cargo test --example t2_lora_aqlm -- falsifier_holds_on_fixture"
    if_fails: "lora_quantized regression on bundled fixture"

kani_harnesses:
  - id: KANI-FT-T2_LORA_AQLM-001
    obligation: "main() is total"
    bound: 4
    strategy: bounded_int
    harness: "kani_harnesses::ft_t2_lora_aqlm_total"
  - id: KANI-FT-T2_LORA_AQLM-002
    obligation: "main() is deterministic"
    bound: 4
    strategy: bounded_int
    harness: "kani_harnesses::ft_t2_lora_aqlm_deterministic"
  - id: KANI-FT-T2_LORA_AQLM-003
    obligation: "AQLM 2-bit base + LoRA: post-merge perplexity ≤ FP16 baseline + 10%"
    bound: 4
    strategy: bounded_int
    harness: "kani_harnesses::ft_t2_lora_aqlm_convergence"

qa_gate:
  id: F-FT-T2_LORA_AQLM-001
  name: "Fine-tuning t2_lora_aqlm contract"
  description: "lora_quantized on llama: AQLM 2-bit base + LoRA: post-merge perplexity ≤ FP16 baseline + 10%"
  checks:
    - main_is_total
    - deterministic_across_runs
    - falsifier_holds_on_fixture
  pass_criteria: "All 3 falsification tests pass on bundled fixture"
