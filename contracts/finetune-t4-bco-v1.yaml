metadata:
  version: "1.0.0"
  created: "2026-05-10"
  author: "PAIML Engineering"
  description: "Fine-tuning recipe t4_bco: bco on llama"
  references:
    - "docs/specifications/fine-tuning-cookbook.md"
    - "docs/specifications/fine-tuning-cookbook/manifest.yaml#t4_bco"
  depends_on:
    - "recipe-iiur-v1"
  tags:
    - finetune
    - tier_4
    - bco
    - llama

kernel_structure:
  phases:
    - name: setup
      description: "Construct RecipeContext; load fixture"
      invariant: "context.temp_dir empty; fixture path readable"
    - name: train
      description: "Run apr finetune (or technique-specific entry point)"
      invariant: "Training is deterministic for fixed seed=42"
    - name: evaluate
      description: "Run the recipe metric (loss, perplexity, accuracy, etc.)"
      invariant: "Metric is finite; no NaN/Inf"
    - name: verify
      description: "Assert the falsifier"
      invariant: "Two consecutive runs produce equal verdict"
    - name: teardown
      description: "Drop RecipeContext"
      invariant: "context.temp_dir cleaned up"

equations:
  totality:
    formula: "main() returns Result<()> for any well-formed fixture"
    domain: "fixture: Path"
    codomain: "Result<()>"
    invariants:
      - "main() is total — no panic for any well-formed input"
    preconditions:
      - "fixture is valid UTF-8 JSONL or CSV"
    postconditions:
      - "Result is Ok or Err; no panic"
    lean_theorem: "ProvableContracts.Finetune.T4Bco.Totality"
    tolerance: 0
    lean:
      theorem: "ProvableContracts.Finetune.T4Bco.Totality"
      status: proved
      module: "lean/ProvableContracts/Finetune/T4Bco.lean"

  determinism:
    formula: "main() with seed=42 produces equal output across runs"
    domain: "seed: u64, fixture: Path"
    codomain: "verdict: Hash"
    invariants:
      - "Two consecutive main() calls with seed=42 produce byte-equal output"
    preconditions:
      - "seed is fixed; fixture is identical"
    postconditions:
      - "verdict_1 == verdict_2"
    lean_theorem: "ProvableContracts.Finetune.T4Bco.Determinism"
    tolerance: 0
    lean:
      theorem: "ProvableContracts.Finetune.T4Bco.Determinism"
      status: proved
      module: "lean/ProvableContracts/Finetune/T4Bco.lean"

  convergence:
    formula: "BCO Binary Classifier Optimization: thumb-up/down feedback model classifier accuracy ≥ 0.7"
    domain: "fixture: Path, training_steps: u32"
    codomain: "metric: f32"
    invariants:
      - "BCO Binary Classifier Optimization: thumb-up/down feedback model classifier accuracy ≥ 0.7"
    preconditions:
      - "fixture is bounded, deterministic"
    postconditions:
      - "the falsifiable claim above holds on the bundled fixture"
    lean_theorem: "ProvableContracts.Finetune.T4Bco.Convergence"
    tolerance: 0
    lean:
      theorem: "ProvableContracts.Finetune.T4Bco.Convergence"
      status: not-applicable
      module: "lean/ProvableContracts/Finetune/T4Bco.lean"

proof_obligations:
  - type: invariant
    property: "main() is total"
    formal: "for all fixture: Path. main() ↦ Result<()> (no panic)"
    tolerance: 0.0
    applies_to: totality
    lean:
      theorem: "ProvableContracts.Finetune.T4Bco.Totality"
      status: proved
      module: "ProvableContracts.Finetune.T4Bco"
  - type: invariant
    property: "main() is deterministic"
    formal: "main(seed=42) == main(seed=42)"
    tolerance: 0.0
    applies_to: determinism
    lean:
      theorem: "ProvableContracts.Finetune.T4Bco.Determinism"
      status: proved
      module: "ProvableContracts.Finetune.T4Bco"
  - type: invariant
    property: "BCO Binary Classifier Optimization: thumb-up/down feedback model classifier accuracy ≥ 0.7"
    formal: "BCO Binary Classifier Optimization: thumb-up/down feedback model classifier accuracy ≥ 0.7"
    tolerance: 0.0
    applies_to: convergence
    lean:
      theorem: "ProvableContracts.Finetune.T4Bco.Convergence"
      status: not-applicable
      module: "ProvableContracts.Finetune.T4Bco"

falsification_tests:
  - id: FALSIFY-FT-T4_BCO-001
    rule: "main() is total on the bundled fixture"
    prediction: "main() returns Ok(()) on the bundled fixture"
    test: "cargo test --example t4_bco -- recipe_runs"
    if_fails: "Recipe panics or returns Err on the bundled fixture"
  - id: FALSIFY-FT-T4_BCO-002
    rule: "Two consecutive runs produce equal output (determinism)"
    prediction: "main() with seed=42 produces equal verdict across two runs"
    test: "cargo test --example t4_bco -- deterministic_across_runs"
    if_fails: "Non-determinism leaked (clock, RNG, threading)"
  - id: FALSIFY-FT-T4_BCO-003
    rule: "BCO Binary Classifier Optimization: thumb-up/down feedback model classifier accuracy ≥ 0.7"
    prediction: "BCO Binary Classifier Optimization: thumb-up/down feedback model classifier accuracy ≥ 0.7"
    test: "cargo test --example t4_bco -- falsifier_holds_on_fixture"
    if_fails: "bco regression on bundled fixture"

kani_harnesses:
  - id: KANI-FT-T4_BCO-001
    obligation: "main() is total"
    bound: 4
    strategy: bounded_int
    harness: "kani_harnesses::ft_t4_bco_total"
  - id: KANI-FT-T4_BCO-002
    obligation: "main() is deterministic"
    bound: 4
    strategy: bounded_int
    harness: "kani_harnesses::ft_t4_bco_deterministic"
  - id: KANI-FT-T4_BCO-003
    obligation: "BCO Binary Classifier Optimization: thumb-up/down feedback model classifier accuracy ≥ 0.7"
    bound: 4
    strategy: bounded_int
    harness: "kani_harnesses::ft_t4_bco_convergence"

qa_gate:
  id: F-FT-T4_BCO-001
  name: "Fine-tuning t4_bco contract"
  description: "bco on llama: BCO Binary Classifier Optimization: thumb-up/down feedback model classifier accuracy ≥ 0.7"
  checks:
    - main_is_total
    - deterministic_across_runs
    - falsifier_holds_on_fixture
  pass_criteria: "All 3 falsification tests pass on bundled fixture"
