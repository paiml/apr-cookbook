# GPT-NeoX Synthetic Micro-Fixture (PMAT-305)
2-layer GPT-NeoX config. Discriminator: use_parallel_residual=true
(GPT-NeoX runs attention and FFN in parallel rather than sequentially)
plus rotary_pct partial-rotary embedding.
