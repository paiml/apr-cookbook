name: Recipe Table

on:
  pull_request:
    branches: [main, master]
    paths:
      - 'Cargo.toml'
      - 'examples/**'
      - 'README.md'
      - 'scripts/generate-recipe-table.sh'
  push:
    branches: [main, master]
    paths:
      - 'Cargo.toml'
      - 'examples/**'
      - 'README.md'
      - 'scripts/generate-recipe-table.sh'
  workflow_dispatch:

jobs:
  check-recipe-table:
    name: Verify recipe table is deterministic
    runs-on: [self-hosted, Linux, clean-room]   # fleet clean-room pool, arch-neutral (#441)
    steps:
      - uses: actions/checkout@v4

      - name: Verify recipe table matches source
        run: ./scripts/generate-recipe-table.sh --check
